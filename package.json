{
  "name": "microbit-firebase-bridge",
  "version": "1.0.0",
  "description": "Web and Windows bridge tools that upload micro:bit serial JSON data to Firebase Realtime Database.",
  "type": "module",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js",
    "web": "node web-server.js",
    "ports": "node list-ports.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serialport": "^12.0.0"
  }
}
