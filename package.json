{
  "name": "microbit-firebase-bridge",
  "version": "1.0.0",
  "description": "Windows bridge program that uploads micro:bit serial JSON data to Firebase Realtime Database.",
  "type": "module",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js",
    "ports": "node list-ports.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serialport": "^12.0.0"
  }
}
